/// All functions with this annotation will be combined and generated to the `worker.js`
///  inside the `web` folder.
const isolatesHelperWorker = _IsolatesHelperWorker();

/// Worker annotation
class _IsolatesHelperWorker {
  const _IsolatesHelperWorker();
}
